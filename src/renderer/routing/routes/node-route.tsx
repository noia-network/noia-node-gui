import * as React from "react";

import { NodeRouteContainer } from "@renderer/modules/node-route/node-route-module";

export class NodeRoute extends React.Component {
    public render(): JSX.Element {
        return <NodeRouteContainer />;
    }
}
